FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml .
COPY src/ src/
COPY docker/constraints-api.txt .
RUN pip install -e . -c constraints-api.txt --extra-index-url https://download.pytorch.org/whl/cpu
CMD ["uvicorn", "healthcare_nlp.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
